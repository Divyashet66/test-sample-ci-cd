apiVersion: v1
kind: Service
metadata:
  name: {{ include "nw18-java-hello.fullname" . }}
  labels:
    {{- include "nw18-java-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nw18-java-hello.selectorLabels" . | nindent 4 }}
